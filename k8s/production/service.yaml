apiVersion: v1
kind: Service
metadata:
  name: patient-management-deployment-service
spec:
  selector:
    app: patient-management-service-deployment-prod
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: NodePort
  